// Code generated by tfgen. DO NOT EDIT.

package {{.Package}}

import (
	"context"
	"encoding/json"
	"fmt"
	"math"
	"reflect"
	"strconv"
	"time"

	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-framework/types/basetypes"

	"{{.Conv.SDKPkg}}"
{{- with .Conv.CustomPkg}}
	custom "{{.}}"
{{- end}}
)
{{- $sdk := .Conv.SDKName}}
{{- range .Roots}}
{{- $name := .Name}}
{{- with .Obj}}

// Expand{{$name}} returns the SDK value of m. A null value is not sent, so
// false, 0, "", [] and {} are sent as values. p is the path of m in the
// schema, for the diagnostics.
func Expand{{$name}}(ctx context.Context, p path.Path, m *{{.Model}}) (*{{$sdk}}.{{.SDK}}, diag.Diagnostics) {
	var diags diag.Diagnostics
	out := expand{{.Func}}(ctx, p, m, &diags)
	return out, diags
}

// Flatten{{$name}} returns the Terraform model of v. A value that v does not
// have is null. p is the path of the model in the schema, for the
// diagnostics.
func Flatten{{$name}}(ctx context.Context, p path.Path, v *{{$sdk}}.{{.SDK}}) (*{{.Model}}, diag.Diagnostics) {
	var diags diag.Diagnostics
	out := flatten{{.Func}}(ctx, p, v, &diags)
	return out, diags
}

{{- end}}
{{- end}}
{{template "convObjects" .Conv}}
{{template "convHelpers" .}}
