// Code generated by tfgen. DO NOT EDIT.

{{- if not .Roots}}

// Package {{.Package}} has Terraform names of API enums for handwritten
// resources. It is written by: tfgen {{.Command}}
{{- end}}
package {{.Package}}

import (
	"{{.SDKPkg}}"
)
{{range .Enums}}
// {{.Name}}ByName maps the Terraform name of each {{.Schema}} value to the API
// value. The name is the API value in lower case, without its prefix
// ({{if .Prefix}}{{.Prefix}}{{else}}none{{end}}) and without the suffix _OR_UNSPECIFIED or _UNSPECIFIED.
{{- if .Overridden}}
// The overrides file gives some values other names, and can name the value
// that only means "not set"; without a name, that value is null.
{{- else}}
// The value that only means "not set" has no name.
{{- end}}
var {{.Name}}ByName = map[string]{{.SDK}}{
{{- range .Items}}
	{{printf "%q" .TFName}}: {{.Const}},
{{- end}}
}

// {{.Name}}Names are the Terraform names of {{.Schema}}, in API order, for
// example for stringvalidator.OneOf({{.Name}}Names...).
var {{.Name}}Names = []string{
{{- range .Items}}
{{- if not .ReadOnly}}
	{{printf "%q" .TFName}},
{{- end}}
{{- end}}
}

// {{.Name}}Name returns the Terraform name of the API value v, and false when
// v has none (an unknown value, or the value that only means "not set").
func {{.Name}}Name(v {{.SDK}}) (string, bool) {
	switch v {
{{- range .Items}}
	case {{.Const}}:
		return {{printf "%q" .TFName}}, true
{{- end}}
	}
	return "", false
}
{{end}}
