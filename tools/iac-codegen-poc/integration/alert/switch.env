# Inputs of integration/switch.sh and integration/live.sh for
# coralogix_alert (a partial switch: schedule and four custom fields stay
# handwritten).
ROOT=AlertDefProperties
TAG="Alert definitions service"
PKG=internal/provider/alerts/alertdeftypes      # the generated package
TEST_DIR=internal/provider/alerts               # the handwritten package, for the checks
LIVE_ADDRESS=coralogix_alert.example            # the resource that live.sh imports
