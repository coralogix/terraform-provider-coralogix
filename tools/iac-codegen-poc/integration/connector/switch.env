# Inputs of integration/switch.sh and integration/live.sh for
# coralogix_connector (a partial switch: connector_config stays handwritten).
ROOT=Connector
TAG="Connectors service"
PKG=internal/provider/notifications/connectortypes      # the generated package
TEST_DIR=internal/provider/notifications                # the handwritten package, for the checks
LIVE_ADDRESS=coralogix_connector.example                # the resource that live.sh imports
